# Wersja 0.4.1

## Zmiany
- Usunięto ręczne linkowanie `react-native-image-picker` z konfiguracji Gradle na Androidzie, aby korzystać z autolinkingu React Native.
- Dodano logowanie `ImagePickerManager` oraz korzystanie z nowego API `launchImageLibrary`, żeby potwierdzić poprawną inicjalizację modułu.
- Zweryfikowano zależność `react-native-image-picker` w konfiguracji projektu.
